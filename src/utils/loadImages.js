// export const loadImages = (directory) => {
//   const images = import.meta.glob(`${directory}/**/*.{png,jpg,jpeg,svg}`);
//   const imageMap = {};

//   for (const path in images) {
//     const key = path.replace(`${directory}/`, "");
//     imageMap[key] = images[path];
//   }

//   return imageMap;
// };
